<%= simple_form_for [@course, @student, @exam] do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="row">
    <%= form.input :note, wrapper_html: { class: 'col-6' } %>
  </div>
   <div class="row">
    <%= form.input :evaluation, collection: @course.evaluations, value_method: :id, wrapper_html: { class: 'col-6' } %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-success' %>

  </div>
<% end %>
